#include <stdio.h>

int main(){
    int i, matriz;

    for (i = 0; i < ; i++)
    {
        printf("%d da matriz A: ", i+1);
        scanf("%", &); 
    }
    
    for (i = 0; i < ; i++)
    {
        printf("% \n", ); 
    }

    return 0;
}